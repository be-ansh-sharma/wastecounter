import { COLOR } from 'global/styles';
import { StyleSheet } from 'react-native';


export default StyleSheet.create({
  subHeader: {
    color: COLOR.primary,
  },
  text: {
    color: COLOR.accent,
    alignSelf: 'center',
  },
});
